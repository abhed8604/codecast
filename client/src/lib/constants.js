/** Shared tuning constants (kept in one place so replay/recording stay in sync). */
export const KEYFRAME_INTERVAL = 40
export const CLOCK_THROTTLE_MS = 60
export const SCRUB_THROTTLE_MS = 50
export const RECORDER_POLL_MS = 100
export const SEEK_TOLERANCE_MS = 2
