PORT=3002
# Resolved relative to server/db (see server/db/init.js), so the dev
# server works regardless of the working directory it is launched from.
DB_PATH=./codecast.db
RUNNER_CONTAINER=codecast-runner
